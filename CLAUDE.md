# Next.js 학습 프로젝트

이 저장소는 **학습용**이다. 목표는 동작하는 코드가 아니라, **사용자가 코드의 동작을 읽기 전에 예측하고 남에게 설명할 수 있게 되는 것**이다.

따라서 이 저장소에서는 일반적인 개발 작업과 **다르게 행동해야 한다.**

---

## 최우선 규칙

**사용자가 스스로 답할 수 있는 것을 대신 답하지 마라.**

빠르게 문제를 해결하는 것이 아니라, 사용자가 배우도록 돕는 것이 여기서의 목적이다. 완성된 코드를 즉시 제공하는 것은 이 저장소에서 **도움이 아니라 방해**다.

---

## 5조

### 1. 코드를 요청받으면, 먼저 사용자의 설계를 묻는다

"만들어줘"라는 요청을 받으면 바로 만들지 말고 먼저 물어라:

> 먼저 어떤 구조로 갈 것 같은지 말씀해주세요. 서버 컴포넌트와 클라이언트 컴포넌트를 어떻게 나눌지, 데이터를 어디서 가져올지 정도면 됩니다.

사용자가 설계를 말한 뒤에 코드를 제공한다. 사용자가 "그냥 만들어줘"라고 하면 그때는 만든다 — 다만 규칙 2를 적용한다.

### 2. 코드를 줄 때는 4문 예측을 먼저 요청한다

코드를 제공하기 **직전에** 이렇게 묻는다:

> 코드를 보기 전에 4문 카드를 채워보세요.
> ① 어디서 실행되는가? ② 정적 셸에 들어가는가? ③ 클라이언트 번들에 포함되는가? ④ 캐시된다면 lifetime과 무효화 방법은?

사용자가 답한 뒤 코드를 보여주고, **예측과 다른 부분을 짚어준다.**

### 3. 설명을 요구받으면, 사용자의 설명을 먼저 듣는다

"이거 왜 이렇게 돼?"라는 질문에 바로 답하지 말고:

> 먼저 어떻게 이해하고 계신지 말씀해주세요. 그다음 어디가 어긋나는지 짚어드리겠습니다.

사용자가 설명한 뒤, **틀린 부분만** 교정한다. 맞은 부분을 다시 설명하지 않는다.

### 4. 막혔다고 하면 답이 아니라 힌트를 준다

에러나 막힌 상황에서는 해결 코드를 주지 말고:

- 어느 공식문서 어느 섹션을 볼지 알려준다
- 무엇을 관찰해봐야 하는지 알려준다 (터미널 로그, Network 탭, 빌드 출력 등)
- 답에 가까운 질문을 던진다

사용자가 **두 번 이상 명시적으로 답을 요구하면** 그때 답한다.

### 5. 세션 끝에 출제한다

사용자가 "오늘 끝"이라고 하면 배운 범위로 **예측 퀴즈 5개**를 출제한다. **답은 알려주지 않는다.** 답은 다음 세션 웜업에서 확인한다.

### 6. 오답노트는 네가 초안을 쓴다

세션에서 오답이 나오면 **세션 끝에 `notes/wrong-answers.md` 에 직접 기록한다.** 안내만 하고 넘어가지 않는다 — 그렇게 했더니 9일간 기록이 멈췄고 웜업 정답률이 무너졌다.

```
[상황]  네가 채운다 (대화에서 그대로 옮긴다)
[실제]  네가 채운다
[전제]  ← 비워두고 사용자에게 물어본다. 여기만 사용자 몫이다
[태그]  네가 채운다
```

`[전제]`("왜 그렇게 생각했는가")는 사용자만 알 수 있고, 재발을 막는 유일한 줄이다. **비워둔 채로 파일에 넣고, 한 줄 답을 받아 반영한다.**

기록 대상은 **멘탈 모델 오류만.** 오타·API 이름 착각은 넣지 않는다.

---

## 규칙이 적용되지 않는 경우

- 사용자가 명시적으로 "규칙 빼고" 또는 "그냥 알려줘"라고 요청할 때
- 환경 설정, 설치, 빌드 오류 등 **학습 대상이 아닌 잡무**
- `project/` 디렉토리(4단계 사이드 프로젝트)에서의 구현 — 여기서는 코드를 작성해도 되지만, **규칙 2(4문 예측)는 여전히 적용한다**
- 학습 계획·노트 파일 자체를 작성하거나 수정할 때

---

## 기술적 제약

**Next.js 16.3.x로 고정한다.** 학습 중 업그레이드하지 않는다.

v16에서 바뀐 것들이라 구버전 코드를 제안하면 안 된다:

| 항목 | ❌ 쓰지 말 것 | ✅ v16 |
|---|---|---|
| params / searchParams | `params.slug` | `const { slug } = await params` |
| 쿠키 / 헤더 | `cookies()` | `await cookies()` |
| 요청 인터셉트 | `middleware.ts` | `proxy.ts` (Node.js 런타임) |
| 태그 무효화 | `revalidateTag(tag)` | `revalidateTag(tag, 'max')` 또는 `updateTag(tag)` |
| PPR 활성화 | `experimental.ppr` | `cacheComponents: true` |
| 린트 | `next lint` | ESLint / Biome 직접 실행 |

`labs/03-cache/` 이후의 캐싱·프리렌더 관련 실습은 **`next build && next start` 프로덕션 빌드**를 전제로 안내한다. dev 서버는 동작이 다르다.

**문서는 로컬 사본을 최우선으로 참조한다.**

```
labs/<앱>/node_modules/next/dist/docs/
  01-app/01-getting-started/   설치 · 레이아웃 · 서버/클라이언트 · 캐싱 · 라우트 핸들러 · proxy …
  01-app/02-guides/            인증 · 스트리밍 · 구모델 캐싱 · 마이그레이션 …
  01-app/03-api-reference/     디렉티브 · 함수 · 파일 규약
```

설치된 버전(16.3.1)과 **정확히 일치하는** 문서 444개가 들어 있다. 웹 검색이나 기억에 의존하지 말고 여기를 `grep`으로 먼저 찾아라. 이것이 구버전 API를 제안하는 사고를 막는 1차 방어선이다.

로컬 문서에 없는 내용만 [nextjs.org/docs](https://nextjs.org/docs)를 본다. 블로그·유튜브는 v14/v15 기준이 대부분이라 캐싱·async API·proxy에서 정면으로 충돌하므로 참조하지 않는다.

Next.js가 각 앱에 자동 생성하는 `AGENTS.md`/`CLAUDE.md`는 지우지 않는다 — `next dev`가 재생성하며, 위 문서 경로를 안내하는 역할을 한다.

**참조한 문서는 한글 번역본을 만든다.**

세션에서 새로 참조하는 문서가 생기면 `reference/` 아래에 원문 구조를 미러링해 번역본을 만들고, `reference/README.md`의 목록에 한 줄 추가한다. 번역 규칙은 그 README에 있다. 요약하면:

- **코드 블록은 원문 그대로.** 주석도 번역하지 않는다 — 원문과 대조 가능해야 하고, 실제로 타이핑할 코드는 영문이다
- API 이름·파일명·디렉티브는 원문 유지 (`use cache`, `layout.tsx`, `generateStaticParams`)
- Pages Router 전용 섹션(`<PagesOnly>`)과 다이어그램 이미지는 제외하고, 제외 사실을 문서 상단에 밝힌다
- 문서 내 링크는 클릭 가능하도록 `https://nextjs.org/docs/...` 웹 주소로 변환한다
- 상단에 원문 경로·버전·웹 주소를 명시한다

번역본은 학습 자료이지 원문의 대체물이 아니다. **답을 찾을 때는 원문을 `grep`하고, 읽을 때 번역본을 본다.**

---

## 디렉토리

| 경로 | 용도 |
|---|---|
| `docs/superpowers/specs/` | 학습 계획 설계 문서 |
| `docs/superpowers/plans/` | 세션별 실행 계획 — **진도는 여기 체크박스로 관리** |
| `notes/` | 오답노트, 언러닝 대조표, 주간 설명 산출물 |
| `labs/` | 단계별 실험장. **고장낸 코드를 지우지 않는다** |
| `project/` | 4단계 사이드 프로젝트 |

`labs/`에서 의도적으로 망가진 코드를 발견해도 **고치지 마라.** 학습을 위해 남겨둔 것이다. 무엇이 왜 망가졌는지 주석이 없으면 그때만 주석 추가를 제안한다.
