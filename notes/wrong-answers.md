# 오답노트

## 기록 규칙

**멘탈 모델 오류만 적는다.** 오타, API 이름 착각, 단순 실수는 적지 않는다. 여기에 잡동사니가 쌓이면 다시 읽지 않게 된다.

**`[전제]` 필드를 비워두지 않는다.** 틀린 답이 아니라 **틀린 전제**를 고쳐야 재발하지 않는다. 이 줄을 쓰기 어렵다면 아직 왜 틀렸는지 모르는 것이고, 그건 그 자체로 중요한 신호다.

## 태그

| 태그 | 범위 |
|---|---|
| `#rsc경계` | 서버/클라이언트 구분, `'use client'`, 직렬화, 컴포지션 |
| `#프리렌더` | 정적 셸, PPR, Suspense 경계, `connection()` |
| `#캐싱` | `use cache`, `cacheLife`, 무효화 3종 |
| `#서버실행` | Route Handlers, Server Actions, 쿠키/헤더, proxy |
| `#라우팅` | 파일 규약, 중첩, 동적 세그먼트, 네비게이션 |
| `#언러닝` | Pages Router 습관의 잔재 → `unlearning.md`로 옮긴다 |

## 주기적으로 할 것

- **주 1회** — 이번 주 항목을 다시 읽고 설명 산출물 주제를 고른다
- **단계 종료 시** — 태그별로 집계한다. 한 태그에 몰려 있으면 그 영역을 다시 본다
- **`#언러닝` 항목** — `unlearning.md`로 옮기고 여기서는 지운다

---

## 기록

### 예시 (형식 참고용 — 첫 항목 기록 후 지운다)

```
[상황]  'use client'가 붙은 Header.tsx가 Logo.tsx를 import할 때,
        Logo.tsx에는 지시어가 없으므로 서버 컴포넌트로 남을 것이라 예측했다.
[실제]  Logo.tsx도 클라이언트 번들에 포함됐다.
[전제]  'use client'를 "이 파일 하나를 클라이언트로 만드는 표시"라고 이해하고 있었다.
        실제로는 "여기부터 아래 import 그래프 전체"에 대한 경계 선언이다.
[태그]  #rsc경계
```

---

<!-- 아래에 새 항목을 추가한다. 최신이 위로 오게 한다. -->
### 16일차 (9/3 · S30 스트리밍과 Suspense)
[상황]  Suspense 가 없을 때 "제목만 즉시 뜨고 나머지는 3초 뒤"라고 예측
[실제]  제목조차 3초 동안 안 온다. await 하나가 페이지 전체를 막는다.
        측정: Suspense 없음 → 3.04s 에 전부 도착 / 있음 → 0.04s 에 셸 도착
[전제]  ← 직접 채울 것
[태그]  #프리렌더

[상황]  Suspense 안에서 에러가 나면 "Suspense 로 감싸진 부분만" 대체될 것으로 예측
[실제]  error.tsx 안쪽 전체가 대체된다. 문제없던 컴포넌트까지 사라진다.
        error 가 Suspense 보다 바깥이므로 대체 범위도 그만큼 넓다
[전제]  ← 직접 채울 것
[태그]  #프리렌더

주: "같은 폴더 error.tsx 가 잡는다"(앞부분)는 맞췄다. 계층도는 잡혔고
    대체 "범위"까지 밀지 않은 것.

### 15일차 (9/2 · S28 Draft Mode)
[상황]  Draft Mode 를 켜도 "정적 페이지를 그대로 보여준다"고 예측.
        쿠키가 유출돼도 "보안 장치가 있어서 못 볼 것"이라 예측
[실제]  매 요청마다 새로 렌더링된다. 쿠키 이름이 __prerender_bypass —
        프리렌더를 우회하라는 뜻이다.
        쿠키만 있으면 다른 클라이언트도 초안을 그대로 본다.
        게다가 /api/draft 를 아무나 호출하면 쿠키를 발급받는다
[전제]  Draft Mode 의 목적 자체를 몰랐고, "정적 페이지"라는 말에서
        "변하지 않는다"를 떠올렸다. 초안을 보여주려면 새로 렌더링해야 한다는
        당연한 귀결까지 밀지 않았다
[태그]  #서버실행 #프리렌더

주: Q2(서버 컴포넌트에서 draft.enable() 은 에러)는 S25 기준을 새 API 에
    정확히 적용해서 맞췄다. 기준이 자리잡고 있다는 신호.

### 14일차 (9/2 · S27 환경변수)
[상황]  NEXT_PUBLIC_ 값이 "서버가 내려줄 것"이라 예측(Q2). 그런데 같은 세션 Q3 에서는
        "새로 빌드해야 값이 바뀐다"고 답했다 — 두 답이 서로 모순
[실제]  빌드 타임에 값 문자열로 치환된다. 클라이언트 번들 코드에서 확인:
        NEXT_PUBLIC_ 은 n="https://api.example.com" 로 박히고,
        DB_PASSWORD 는 process.env.DB_PASSWORD 조회로 그대로 남는다
[전제]  Q2 를 먼저 쓰고 시간이 지나 Q3 을 쓰면서 "빌드된 값"이 기억났는데,
        앞 답을 고치지 않았다. 모르는 게 아니라 제출 전에 훑어보지 않은 것.
        다섯 번 반복된 "자기 답끼리 모순" 패턴의 정체가 이것
[태그]  #서버실행

주: 자기 답끼리 모순되는 패턴이 다섯 번째다.
    S4 / 졸업시험 / S18 / S20 / S27.
    Q3 의 답이 Q2 를 반박하고 있었고, 훑어보기만 했어도 잡혔다.

[상황]  Q1 에서 클라이언트 컴포넌트의 서버 전용 env 를 "빈 문자열"로 예측
[실제]  절반만 맞다. 클라이언트 번들에서는 치환되지만,
        SSR 경로에서는 process.env 가 실제로 읽혀 값이 HTML 에 박힌다.
        JS 번들 0회 / HTML 3개 — 번들만 보면 안전해 보이지만 노출됐다
[전제]  "NEXT_PUBLIC_ 없으면 빈 문자열"이라고 학습한 내용을 그대로 적용했다.
        그 설명이 클라이언트 번들에만 해당한다는 단서를 놓쳤다.
        SSR 경로가 따로 있다는 걸 고려하지 않았다
[태그]  #서버실행

### 13일차 (9/2 · S26 proxy.ts)
[상황]  proxy 가 "페이지 렌더링 후"에 실행되고, 따라서 쿠키를 "쓸 수 없다"고 예측.
        정적 파일 요청에는 "실행되지 않는다"고 예측
[실제]  렌더링 전에 실행된다 (요청을 가로채는 것이므로). 그래서 쿠키를 쓸 수 있고,
        set-cookie 헤더가 실제로 발급됐다. 정적 파일 포함 모든 요청에 실행된다
[전제]  처음 직관은 "proxy 니까 렌더링 전"으로 맞았는데,
        "렌더링 후에 서버 동작이 실행된다"는 잘못된 전제로 추론해서 뒤집었다.
        렌더링 자체가 서버 동작의 일부인데 순서를 반대로 잡았다.
        직관이 맞는데 논리로 덮어쓰는 경우도 있다
[태그]  #서버실행

주: Q2(쿠키 쓰기)는 Q1(실행 시점)이 틀린 결과다. S25 에서 세운 기준
    "응답이 시작되기 전인가"는 정확히 적용했으나, proxy 의 위치를 잘못 잡았다.
    기준은 맞고 입력이 틀린 경우.

### 12일차 (9/1 · S25 cookies / headers)
[상황]  서버 컴포넌트에서 쿠키를 못 쓰는 이유를 "서버에 set 권한이 없다"로 예측.
        쓰려면 "클라이언트 컴포넌트에서 해야 한다"고 답
[실제]  권한이 아니라 타이밍 문제. 헤더는 body 보다 먼저 전송되는데
        서버 컴포넌트는 body 를 만드는 중이라 되돌아가 헤더를 끼워넣을 수 없다.
        쓰는 곳은 Server Action 또는 Route Handler — 응답이 시작되기 전이다.
        에러 메시지가 직접 알려준다: "can only be modified in a Server Action or Route Handler"
[전제]  CORS·HttpOnly 등 브라우저 보안 정책을 계속 다뤄와서 "권한" 프레임으로 기울었다.
        실제로는 HTTP 응답의 구조(헤더가 body보다 먼저)에서 나오는 타이밍 제약이었다
[태그]  #서버실행

주: 힌트(헤더/body 순서, S15 스트림)를 준 뒤에는 한 문장으로 정확히 답했다.
    "이미 body를 보내서 먼저 전송된 header를 끼워넣을 수 없다"

### 11일차 (9/1 · S23 Server Action 보안)
[상황]  안전하게 만들려면 "Page 부분에 신원을 확인할 코드를 추가해야 될 것 같다"고 답
[실제]  Page 가 아니라 액션 함수 안에 넣어야 한다. Server Action 호출은
        page.tsx 를 거치지 않고 액션으로 직행하므로 page 의 검사를 통과하지 않는다.
        curl 로 쿠키·버튼 없이 호출해 사용자 전체를 삭제하는 데 성공
[전제]  React 에서 페이지 진입 시 권한을 체크하던 습관 때문이다.
        CSR 에서는 페이지가 관문이었고 그 아래는 전부 클라이언트였다.
        RSC 에서는 페이지를 거치지 않는 진입로(Server Action)가 새로 생겼다
[태그]  #서버실행

주: Q1(자동 인증 없음) Q2(호출 가능) Q4(체크 불충분)는 정답.
    Q3 은 결론은 맞았으나 이유가 달랐다 — 브루트포스가 아니라
    액션 ID 가 애초에 클라이언트 코드에 들어 있어 공개된 정보다.

### 10일차 (9/1 · S22 흐름 제어)
[상황]  Server Action 의 redirect 응답을 302 로 예측 (관찰 시에는 304 로 보고)
[실제]  200 OK + x-action-redirect: /flow/done;push 커스텀 헤더.
        이미 POST 응답 중이라 302 를 쓰면 브라우저가 자동으로 따라가버려
        클라이언트 사이드 전환이 무의미해진다
[전제]  ← 직접 채울 것
[태그]  #서버실행

주: Q1(다음 줄 실행 안 됨) Q2(예외 기반) Q4(notFound 도 동일)는 모두 정답.
    Q3 는 조수가 예제에 'use server' 를 빠뜨려 질문이 불완전했다.

### 9일차 (9/1 · S21 폼과 상태)
[상황]  <form action={serverAction}> 에서 액션이 받는 인자를 title: string 으로 예측
[실제]  FormData 객체가 온다. HTML 폼이 원래 FormData 를 만들어 POST 하는 규격을
        그대로 받는 것이고, 그래서 JS 없이도 동작한다 (점진적 향상)
[전제]  FormData 로 넘어간다는 건 이미 알고 있었다. 질문을 "인자의 타입은?"이 아니라
        "어떤 데이터가 들어있나?"로 읽고 title 을 답했다. 지식이 아니라 독해 문제
[태그]  #서버실행

[상황]  폼 제출 시 "form 의 기본 동작이므로 새로고침된다"고 예측
[실제]  JS 를 끄면 새로고침되고, 켜면 Next.js 가 가로채서 새로고침 없이 처리한다.
        같은 코드가 두 경로로 동작한다
[전제]  preventDefault 로 폼 기본 동작을 막을 수 있다는 건 알고 있었는데,
        Next.js 가 같은 일을 대신 해줄 수 있다는 데까지 연결하지 못했다.
        "form 의 기본 동작"만 떠올리고 "누가 개입할 수 있는가"를 생각하지 않았다
[태그]  #서버실행

주: useFormStatus 호출 위치(Q4)는 맞췄다. S3 의 useLinkStatus 제약을 유추 적용한 것.
    "Link 안에 배지를 넣어야 했다" → "form 자식에서 호출해야 한다".
    유추가 통한 사례로, 7일차의 유추 실패(에러 경계 → 라우트 충돌)와 대비된다.

### 8일차 (9/1 · S20 Server Actions)
[상황]  Q1 "함수 본문은 브라우저로 전송되지 않는다"를 맞춘 직후,
        Q3 "Server Action 을 호출해도 네트워크 요청은 나가지 않는다"고 답함
[실제]  현재 페이지 URL 로 POST 가 나간다. Next-Action 헤더에 액션 ID 를 담아
        "이거 실행해줘"라고 부탁하는 구조. 넘어가는 것은 함수가 아니라 ID
[전제]  브라우저로 전송되지 않으니 API 호출이 아니라 "서버가 알아서 처리해서
        값을 내려주는" 것이라 상상했다. 서버에 있는 코드를 실행시키려면
        어떻게든 서버에 말을 걸어야 한다는 걸 생각하지 않았다
[태그]  #서버실행 #관찰방법

주: 자기 답끼리 모순되는 패턴이 네 번째다.
    S4       obj 성공 / date 실패 (둘 다 "새로 만든 객체")
    졸업시험  ① 클라이언트 / ④ 서버라서 (같은 컴포넌트)
    S18      res.json() TypeError / request.json() 빈 값
    S20      본문 없음 / 요청 안 나감
    답을 적은 뒤 서로 충돌하지 않는지 훑는 습관이 필요하다.

[상황]  'use client' 와 'use server' 가 대칭이라고 예측
[실제]  대칭이 아니다. 'use client' 는 경계 선언("여기부터 클라이언트"),
        'use server' 는 노출 선언("클라이언트가 호출할 수 있게 열어라").
        서버 컴포넌트는 'use server' 없이도 서버에서 돈다
[전제]  "대칭"이 정확히 무엇을 묻는지 확인하지 않은 채 답했다.
        질문의 용어가 애매하면 되묻거나 스스로 정의한 뒤 답할 것
        (조수가 "대칭"을 정의 없이 물은 점도 있음)
[태그]  #서버실행

### 7일차 (9/1 · S18 Route Handlers)
[상황]  웜업에서 "res.json() 을 두 번 호출하면 TypeError"를 정답으로 답한 직후,
        같은 세션 S18 예측에서 "request.json() 을 두 번 호출하면 빈 값"이라고 답함
[실제]  같은 TypeError. Response 든 Request 든 body 는 같은 스트림 규격이고
        메시지까지 동일하다 — "Body is unusable: Body has already been read"
[전제]  "이미 읽으면 비워져서 빈 값이 된다"고 기억하고 있었다.
        실제로는 비워지는 게 아니라 잠겨서(bodyUsed: true) 접근이 막히고 TypeError 가 난다.
        ※ 조수가 clone 설명 때 "스트림이 이미 비었으니까요"라고 쓴 표현이 오해를 만들었다.
          정확한 표현은 "소비되어 잠겼다"
[태그]  #서버실행 #관찰방법

주: 같은 패턴이 세 번째다.
    1단계 졸업시험 — "번들에 들어가는가"는 맞추고 "어디서 실행되는가"는 틀림
    오늘        — "res.json()"은 맞추고 "request.json()"은 틀림
    지식이 없는 게 아니라 문제 형태가 바뀌면 꺼내지 못한다.

[상황]  page.tsx 와 route.ts 가 같은 폴더에 있으면 "page.tsx 를 우선으로 잡는다"고 예측
[실제]  우선순위 없이 빌드 에러. 게다가 해당 라우트만이 아니라 앱 전체가 500 이 된다
[전제]  error.tsx 가 "가장 가까운 것이 잡는다"는 규칙을 라우트 충돌에도 유추 적용했다.
        경계 탐색(여러 후보 중 하나를 고름)과 라우트 등록(같은 URL 은 하나여야 함)은
        다른 문제다. 유추가 통하지 않는 지점
[태그]  #라우팅

### 6일차 (8/31 · S15·S16 웜업)
[상황]  body를 두 번 읽으면 "빈 값이 나온다"고 답. 서버가 쿠키의 HttpOnly 여부를 "알 수 있다"고 답
[실제]  TypeError가 발생한다. 서버는 알 수 없다 — 요청 Cookie 헤더에는 이름=값만 온다
[전제]  3일이 지나는 동안 복습을 하지 않았다. 실행 결과를 눈으로 보는 것만으로는
        3일을 못 넘긴다 — 본 것과 남는 것은 다르다
[태그]  #서버실행

주: 두 항목 모두 3일 전 실행 결과로 직접 확인한 것. 5일차 기록이 누락된 구간과 겹친다.

### 5일차 (8/28 · S15·S16)
[상황]  GET에 body를 넣으면 무시될 것으로 예측. 미들웨어 body 소비는 "반환값을 읽으면" 해결될 것으로 예측
[실제]  GET+body는 TypeError. 반환값은 같은 객체라 여전히 못 읽음 — 읽기 전에 clone() 해야 한다
[전제]  예전에 API를 호출했을 때 무시됐다고 기억하고 있었는데, 돌이켜보니
        try/catch를 안 넣었거나 async를 안 써서 에러를 못 본 것이었다.
        "예전에 그랬던 것 같다"는 기억이 잘못된 기대를 만들었다
[태그]  #서버실행

[상황]  Secure 속성이 "쿠키 접근을 막는다"고 답
[실제]  Secure는 HTTPS에서만 전송하도록 전송 채널을 제한. 접근 차단은 HttpOnly
[전제]  세 속성을 "쿠키를 보호하는 것들"로 뭉뚱그렸다. 각각 다른 공격을 막는다
[태그]  #서버실행

### 4일차 (8/26 · S5 · 졸업시험)
[상황]  졸업시험 문제1에서 Icon·format.ts를 누락하고 UserPanel을 잘못 포함.
        문제5에서 Table을 "클라이언트에서 실행"이라 답 (같은 시험 문제1에서는 같은 구조를 제외했음)
[실제]  import는 재귀로 따라가야 하고, prop으로 넘어온 것은 따라가지 않는다.
        "번들에 들어가는가"와 "어디서 실행되는가"는 같은 질문
[전제]  두 질문을 서로 다른 것으로 인식했다. 절차 1을 "번들 목록 만드는 도구"로만
        익혀서, 실행 위치를 묻는 형태로 나오면 절차를 꺼내지 않았다.
        절차는 알면서 적용 범위를 몰랐던 상태
[태그]  #rsc경계

### 3일차 (8/25 · S4 직렬화)
[상황]  date·map·set·promise를 "새로 생성하는 객체/함수라서" 실패로 예측
[실제]  넷 다 성공. 타입까지 보존되어 도착
[전제]  new 가 붙어 있어서 "직접 생성하는 것"이라 보고 plain object 와 다르게 취급했다.
        실제 기준은 new 여부가 아니라 "React 가 복원법을 아는 타입인가"
[태그]  #rsc경계

### 2일차
[상황]  prefetch를 끄면 클릭 시 서버 요청이 생기므로 template이 서버에서 재실행될 것이라 예측
[실제]  요청은 발생했지만 page(🔷)만 렌더링됨. template은 서버에서 실행 안 됨
[전제]  "재마운트 = 서버 재실행"이라고 생각했다. 실제로 재마운트는 클라이언트 층의 일이고, 서버는 클라이언트 전환 시 바뀌는 세그먼트만 렌더링한다
[태그]  #rsc경계

[상황]  template 카운터는 초기화되는데 console.log는 안 찍혀서 모순처럼 보였다
[실제]  서로 다른 층에서 일어난 일. 서버 로그는 서버 실행만 보여준다
[전제]  하나의 도구(터미널)로 모든 층의 동작을 관찰할 수 있다고 가정했다. 앞으로는 "이 현상은 어느 층인가"를 먼저 정하고 도구를 고를 것
[태그]  #관찰방법

### 1일차
┌─────┬──────────────────┬─────────────────────────────┬──────────────────────────────────────────────────────┐
│     │       예측       │            실제             │             전제 (여기가 진짜 기록 대상)             │
├─────┼──────────────────┼─────────────────────────────┼──────────────────────────────────────────────────────┤
│ Q2  │ 브라우저에서     │ 빌드 타임                   │ Q1에서 세운 'use client' 규칙을 같은 파일의 다른     │
│     │ 실행             │                             │ 줄에 적용하지 않음                                   │
├─────┼──────────────────┼─────────────────────────────┼──────────────────────────────────────────────────────┤
│ Q3  │ Link 이동 시 2번 │ 1번                         │ 페이지가 바뀌면 레이아웃도 다시 렌더링된다고 생각    │
├─────┼──────────────────┼─────────────────────────────┼──────────────────────────────────────────────────────┤
│ Q4  │ app/layout.tsx   │ public/next.svg,            │ app/ 안에 있으면 라우트라고 생각 (실제 기준은 위치가 │
│     │                  │ app/favicon.ico             │  아니라 파일 이름)                                   │
└─────┴──────────────────┴─────────────────────────────┴──────────────────────────────────────────────────────┘